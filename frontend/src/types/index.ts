// In a separate file (e.g., frontend/src/types/index.ts) or at the top of your component file
export interface Customer {
    id: number;
    name: string;
    email: string;
    credit: number; // Adjust types based on your actual data structure
}